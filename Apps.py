"""
Data for all the apps in the smart watch
"""
#Alarmclock sets the time that the alarm goes off
class Alarmclock(second, minute, hour, pm, almin, alsec, alhr):
     if alhr == hour:
         if almin == minute:
             if alsec == second:
                 alarm()
    def alarm()
        
